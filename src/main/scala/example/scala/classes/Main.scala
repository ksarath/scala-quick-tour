package example.scala.classes

// Singleton, Type inference, main method (entrypoint)

object Main {
  def main(arguments: Array[String]): Unit = {
    println("Famous Hello World!!!")
    // 1 // expression returning an Int value
  }
}
